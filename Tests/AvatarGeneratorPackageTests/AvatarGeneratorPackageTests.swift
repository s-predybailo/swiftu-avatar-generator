import XCTest
@testable import AvatarGeneratorPackage

final class AvatarGeneratorPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AvatarGeneratorPackage().text, "Hello, World!")
    }
}
